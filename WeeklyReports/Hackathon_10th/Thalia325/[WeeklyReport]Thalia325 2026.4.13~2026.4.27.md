### 姓名

龙小羽

### 实习项目

PaddleOCR+ERNIE应用创新赛

### 本周工作

1. **任务 careerpilot 智能体开发**

   - 扩展后端 API
   - 新增全局异常处理、导出目录静态挂载、更多服务模块
   - 新增大批数据集、导入/补机/评估脚本和说明文档
   - 重构前端页面：学生端、教师端、管理员多个页面重写/新增；新增注册页、结果页、面试页、教师花名册等；删除旧的学生 reports/settings 页面和 EditableReport 组件
   - 后端新增 pypdf/xlrd/openpyxl，前端新增 react-markdown

### 下周工作

1. 对 reports、matching、job_import 这几块做慢查询和耗时 profiling，补数据库索引
2. OCR、报告生成、数据导入放后台任务，前台只轮询状态。
3. 加自动测试、lint、构建、发布前检查，防止“能跑但不可发布”
4. 新增路由统一鉴权、错误码、分页、日志追踪 ID
5. 拆分页面（student/teacher/admin），减少首屏包体积，接口加缓存与重试。

### 导师点评

请联系导师填写
